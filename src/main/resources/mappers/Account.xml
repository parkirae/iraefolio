<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iraefolio.mapper.AccountMapper">

    <select id="accountCheck" resultType="boolean" parameterType="com.iraefolio.domain.AccountEntity">
        SELECT COUNT(*)
        FROM TB_USER
        WHERE USER_ID = #{USER_ID}
    </select>

    <insert id="create" parameterType="com.iraefolio.domain.AccountEntity">
        INSERT INTO TB_USER (USER_ID, USER_PW, USER_NAME)
        VALUES (#{USER_ID}, #{USER_PW}, #{USER_NAME})
    </insert>
</mapper>